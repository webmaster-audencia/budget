{
  "name": "audencia-budget-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard local de suivi budgétaire marketing Audencia – lecture automatique d'un fichier Excel et export PDF.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix frontend",
    "dev:server": "npm run dev --prefix server",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n server,frontend -c blue,green \"npm:dev:server\" \"npm:dev:frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
